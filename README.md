# MP3-Tag-Reader
A command-line application developed in C to read and display metadata from MP3 audio files by parsing ID3 tags. The project extracts information such as song title, artist name, album name, year, genre, and comments using file handling and bitwise operations.
